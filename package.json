{
    "name": "kroki-rs-deps",
    "version": "1.0.0",
    "description": "Dependencies for kroki-rs",
    "dependencies": {
        "@mermaid-js/mermaid-cli": "^11.4.2",
        "vega-cli": "^5.27.0",
        "vega-lite": "^5.23.0",
        "wavedrom-cli": "^2.0.0",
        "bpmn-to-image": "^0.6.0"
    },
    "scripts": {
        "postinstall": "echo 'Dependencies installed. Ensure node_modules/.bin is in your path or configure kroki.toml'"
    }
}